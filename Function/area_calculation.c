#include <stdio.h>

int area(int a, int b);
int main()
{
    int c;
    c = area(5, 4);
    printf("The area is %d", c);
    return 0;
}

int area(int a, int b)
{
    float multi;
    multi = a*b;
}